/*
 * Kournal tablet journal
 *
 * Main configuration defines
 *
 * @author Marek Pikuła <marek@pikula.co>
 * @url https://github.com/Kournal/Kournal
 * @license GPLv2 or later
 */

// Project constants

/**
 * Project name
 */
#define PROJECT_NAME "@PROJECT_NAME@"

/**
 * Project version number
 */
#define PROJECT_VERSION "@PROJECT_VERSION@"

/**
 * Project name with version
 */
#define PROJECT_STRING "@PROJECT_STRING@"

/**
 * Project main website
 */
#define PROJECT_URL "@PROJECT_URL@"


// Git repository info

/**
 * Origin URL of current git repository
 */
#define GIT_ORIGIN_URL "@GIT_ORIGIN_URL@"

/**
 * Origin repository owner
 */
#define GIT_ORIGIN_OWNER "@GIT_ORIGIN_OWNER@"

/**
 * Origin repository name
 */
#define GIT_ORIGIN_REPO "@GIT_ORIGIN_REPO@"

/**
 * Current git branch
 */
#define GIT_BRANCH "@GIT_BRANCH@"

/**
 * URL to git issue tracker basing on origin remote (if git not found set 
 * default tracker)
 */
#define PROJECT_BUGREPORT "@PROJECT_BUGREPORT@"
